.mb-8
  = link_to admin_configuration_path, class: 'text-gray-500 hover:text-sky-300 flex items-center' do
    .h-4.w-4.mr-1
      = render HeroiconComponent.new(:'chevron-left')
    = t('shared.actions.back')

%h2.text-sky-300.uppercase.text-sm.mb-2
  = WeekdayTemplate.model_name.human(count: 2)

= turbo_stream_from('admin_weekday_templates')
= render Admin::WeekdayTemplateListComponent.new(weekday_templates: weekday_templates)
